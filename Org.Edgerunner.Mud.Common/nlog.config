<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
   <targets>
      <target xsi:type="File" name="f" fileName="${specialfolder:folder=ApplicationData}/Moo Udditor/Moo Udditor.log"
              layout="${longdate} ${uppercase:${level}} ${message} ${exception}"
              archiveFileName="${specialfolder:folder=ApplicationData}/Moo Udditor/Moo Udditor.log.{###}"
              archiveNumbering="Rolling" 
              archiveEvery="Day"
              maxArchiveFiles="5" />
   </targets>
   <rules>
      <logger name="*" minlevel="Trace" writeTo="f" />
   </rules>
</nlog>